#!/bin/bash

# Print one Docker context name per line. Used as the settings form's
# optionsCommand so the "Contexts" multiselect offers what docker knows about.

command -v docker >/dev/null 2>&1 || exit 0
docker context ls --format '{{.Name}}' 2>/dev/null
