{
  "name": "dockarchy",
  "version": "0.8.2",
  "private": true,
  "description": "Docker container status for local and remote hosts in the Omarchy bar.",
  "license": "WTFPL",
  "type": "module",
  "scripts": {
    "test": "node --test"
  }
}
